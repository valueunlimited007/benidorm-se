---
export interface Props { width?: string; height?: string; }
const { width = '100%', height = '280px' } = Astro.props;
---
<div style={`display:block; width:${width}; height:${height}; background:#f6f6f6; border:1px dashed #ccc; text-align:center; align-content:center;`}>
  <span>AdSense slot (placeholder). Ersätt med din riktiga AdSense-kod + CMP.</span>
</div>
